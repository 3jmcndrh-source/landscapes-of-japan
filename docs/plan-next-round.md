# 次期改修 ①〜⑧ 実装計画と進捗

起点: `27676a9` / 本番デプロイ `20fc8e65` / 作業ブランチ `feature/next-round`
比較基準: `docs/baseline-before.json`（843枚 / 21県 / 86撮影地 / 25言語 / 写真詳細5,901ページ / トップ初期JS 992KB）

他セッションの `scripts/analytics/daily-report.mjs` は未コミットのまま保全する（今回の差分に入れない）。

## 実装順序

⑧ → ①④ → ②③⑤⑥⑦ の順。⑧のデータ・共通処理を土台にする。

## 進捗

| | 項目 | 状態 | 実体 |
|---|---|---|---|
| ⑧ | 共通データ・共通処理・公開前チェック | **一部完了** | 下記 |
| ① | 統合探索画面 | 未着手 | |
| ② | 撮影地マップ連動 | 未着手 | 座標 `app/loc-points.js` が必要 |
| ③ | 拡大・比較 | 未着手 | |
| ④ | 画像配信 | 未着手 | |
| ⑤ | 地域×テーマ ギャラリー | 未着手 | |
| ⑥ | 共有アルバム | 未着手 | |
| ⑦ | 画像内容検索 | 未着手 | 方式選定が必要 |

### ⑧ の完了分

- `scripts/generate-photo-dims.mjs` → `app/photo-dims.js`
  実画像の寸法 843/843（原本庫の台帳から）。横608 / 縦235 / 不明0。
  縦横の判定閾値（1.05 / 0.95）を1か所に定義。
- `app/photo-model.js`
  写真レコード・URL解決・条件の解釈・絞り込み・並び替えを1か所に集約。
  重いデータ（撮影日・撮影月・タグ・寸法・色・追加日）は `loadFacets()` で
  動的読み込み。既存ページの初期JSは増やさない。
  テーマ判定は既存 `getCollectionPhotos` と全12コレクションで一致を実測。
- `eslint.config.mjs` + `npm run lint`
  `no-undef` を error に。過去に本番を壊した「import漏れ」型を公開前に止める。
  現行コードのエラーは0件（実在の指摘1件を修正済み）。
- `scripts/check-data.mjs` + `npm run check:data`
  写真ID・slug・訳語・生成物の整合を検査。落とすものと知らせるだけを区別。
- `npm run build` の先頭に `npm run check` を組み込み。

### ⑧ の残り

- 写真カード・画像表示の共通部品（④と同時に作る）
- 履歴の保存・復元の共通化（①と同時）
- 主要導線の自動操作確認（①〜⑦が入ってから）
- 生成物の残存ID掃除（撮影日72 / 撮影月72 / アンビエント色79）

### 既知の状態（今回の改修対象ではないが記録）

- 被写体タグが無い掲載写真 50枚（コレクションでは loc フォールバックで拾われる）
- 追加日が分からない写真 397枚（履歴が遡れないぶん。新着には出さない）
