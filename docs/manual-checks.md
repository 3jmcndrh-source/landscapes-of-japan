# 人の手で確かめる項目 (自動では確認できないもの)

自動検証では **DOM とアクセシビリティツリーまで**しか確認できていない項目をここに残す。
**未確認のものを「確認済み」と書かないこと。** 実施したら、その行の記録欄に日付・環境・結果を書く。

対象は本番 `https://landscapes-of-japan.com`。

---

## 1. スクリーンリーダーでの読み上げ

自動側で確認済み: `.ex-status` が `role="status"` (暗黙の `aria-live="polite"`)、
AX ツリーで `ignored:false / live:polite / atomic:true`、`inert` の外、
ライブリージョンに写真カードも件数バッジも入っていない。
**実際に読み上げられるかは未確認。**

環境: NVDA か Windows ナレーターなど。使ったソフトと版を記録欄に書く。

| # | 操作順 | 期待する結果 | 記録欄 (日付 / 環境 / 結果) |
|---|---|---|---|
| 1-1 | `/ja/explore` を開く → 「絞り込み」を開く → 「春」を選ぶ | 「読み込み中」が読み上げられる | |
| 1-2 | そのまま待つ | 件数 (例「347」) が読み上げられる。**「読み込み中」と件数が二重に読まれない** | |
| 1-3 | 通信を切って 1-1 をやり直す (機内モード等) | 「絞り込みに必要なデータを読み込めませんでした」が読み上げられる | |
| 1-4 | 1-3 の状態で Tab キーを送る | 「もう一度試す」ボタンへ到達でき、Enter / Space で押せる | |
| 1-5 | 1-1〜1-3 の各時点でフォーカス位置を確認 | 条件を選んでもフォーカスが**勝手に移動しない** (押したボタンのまま) | |
| 1-6 | 待機中に写真一覧の上をタブ移動 | 待機中の一覧 (`inert`) には**入れない**。写真60枚が読み上げられない | |

参考: W3C ARIA22 (状態通知の確認手順) <https://www.w3.org/WAI/WCAG22/Techniques/aria/ARIA22>

---

## 2. 実機スマートフォン

自動側は **375×812 のエミュレーションのみ**。実機は未確認。
機種・OS・ブラウザ・回線を記録欄に書く。

| # | 操作順 | 期待する結果 | 記録欄 (日付 / 機種・OS・ブラウザ / 結果) |
|---|---|---|---|
| 2-1 | `/ja/explore` を開く | 写真一覧が出る。横スクロールが出ない | |
| 2-2 | 一覧を下までスクロール → 写真を1枚タップ | Lightbox が開く。閉じると元の位置へ戻る | |
| 2-3 | 「絞り込み」→「春」を選ぶ | 件数と一覧が切り替わる。待機が入る場合は前の一覧が残り、薄く表示される | |
| 2-4 | 待機中に前の一覧をタップ | **Lightbox が開かない** (待機中は操作できない) | |
| 2-5 | 条件を解除 | 元の一覧に戻る | |
| 2-6 | 「この言葉で探す」を実行 | 容量の案内が出て、取得が始まる | |
| 2-7 | 取得中に「中止」 | 通信が止まり、「もう一度試す (残り◯MB)」が出る | |
| 2-8 | 「もう一度試す」 | 取得が再開し、結果が出る | |
| 2-9 | 2-6〜2-8 の間にスクロール・Lightbox を操作 | 引っかからずに動く | |
| 2-10 | 日本語IMEで文字を入力し、変換を確定する Enter | **検索が始まらない** (確定だけで実行されない) | |

---

## 3. 記録の書き方

- 実施したら上の表の記録欄に **日付 / 環境 / 結果** を書く。
- 期待と違ったら、その場の画面と操作順を `docs/improvement-24h.md` へ追記する。
- **エミュレーションで代替した場合は「エミュレーション」と明記する。** 実機確認とは呼ばない。
